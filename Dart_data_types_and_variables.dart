void main() {
  //Number int

  var count = 545; // It is inferred as integer automatically by  compiler
  int hexValue = 0xEADEBAEE;
  //Numbers double
  double percentage = 93.3;

  //Strings

  var company = "Google";

  // Boolean
  bool isValid = true;
  var isAlive = false;

  print(hexValue);
  print(company);

  /*NOTE: All data types are objects by default
  Therefore, there initial values are null by default
  print(score);
  print(name);*/
}
